import { render, screen } from "@testing-library/react";
import App from "./App";

// test("renders title", () => {
//   render(<App />);
//   const linkElement = screen.getByText(/Title/i);
//   expect(linkElement).toBeInTheDocument();
// });

// test("renders author", () => {
//   render(<App />);
//   const linkElement = screen.getByText(/Author/i);
//   expect(linkElement).toBeInTheDocument();
// });

// test("renders image", () => {
//   render(<App />);
//   const linkElement = screen.getByAltText(/meme from reddit/i);
//   expect(linkElement).toBeInTheDocument();
// });
